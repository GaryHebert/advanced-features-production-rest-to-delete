import { MachineConfiguration } from './machine-configuration';

describe('MachineConfiguration', () => {
  it('should be defined', () => {
    expect(new MachineConfiguration()).toBeDefined();
  });
});
